# OpenAI Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_API_BASE=https://api.openai.com/v1

# Model Selection
LLM_MODEL=openai:gpt-4

# Alternative models:
# LLM_MODEL=openai:gpt-3.5-turbo  (cheaper, faster)
# LLM_MODEL=openai:gpt-4-turbo    (faster GPT-4)
# LLM_MODEL=openai:gpt-4o         (latest GPT-4 optimized)

# Memory Configuration
# Number of recent messages to keep in short-term memory (PostgreSQL checkpoints)
SHORT_TERM_MESSAGE_LIMIT=30
